
Таблица 4 (диаграмма)
```mermaid
erDiagram
    users {
        INTEGER user_id PK
        TEXT first_name
        TEXT last_name
        TIMESTAMP registered_at
    }
    
    certificate_orders {
        INTEGER order_id PK
        INTEGER user_id FK
        TEXT student_full_name
        TEXT student_group
        TEXT certificate_type
        TEXT student_message
        TEXT status
        TIMESTAMP order_date
        TIMESTAMP processed_date
        TEXT admin_comment
    }
    
    types_of_specialties {
        INTEGER tos_id PK
        TEXT name
    }
    
    specialties {
        INTEGER spec_id PK
        INTEGER tos_id FK
        TEXT code
        TEXT name
        TEXT description
        INTEGER duration_years
        INTEGER budget_places
        INTEGER paid_places
        INTEGER price_per_year
    }
    
    documents {
        INTEGER doc_id PK
        TEXT name
        TEXT description
        BOOLEAN is_required
        INTEGER priority
    }
    
    dormitory {
        INTEGER dorm_id PK
        TEXT type_places
        INTEGER price_per_month
    }
    
    contacts {
        INTEGER contact_id PK
        TEXT phone
        TEXT email
        TEXT address
        TEXT working_hours
        INTEGER priority
    }
    
    important_dates {
        INTEGER date_id PK
        TEXT title
        DATE date_value
        TEXT description
    }
    
    links {
        INTEGER link_id PK
        TEXT name
        TEXT url
        INTEGER priority
    }
    
    users ||--o{ certificate_orders : "один-ко-многим"
    types_of_specialties ||--o{ specialties : "один-ко-многим"

